/* eslint-disable no-unused-vars */
/* eslint-disable react/prop-types */

const Label = ({ children }) => {
  return (
    <label className="text-sm font-medium text-gray-90">{children}</label>
  );
};

export default Label;
